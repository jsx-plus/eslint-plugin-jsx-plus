/**
 * @fileoverview directive names.
 * @author tony7lee
 */

module.exports = {
  DIR_NAME: {
    // conditions
    X_IF: 'x-if',
    X_ELSEIF: 'x-elseif',
    X_ELSE: 'x-else',

    // loop
    X_FOR: 'x-for',

    // memory cache
    X_MEMO: 'x-memo',

    // classNames
    X_CLASS: 'x-class',

    // slot
    X_SLOT: 'x-slot'
  },

  ELE_NAME: {
    FRAGMENT: 'Fragment',
    SLOT: 'slot'
  },

  OPERATORS: {
    IN: 'in'
  }
};
